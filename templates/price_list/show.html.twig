{% extends 'base.html.twig' %}

{% block title %}PriceList{% endblock %}

{% block body %}
    <h1>PriceList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ price_list.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_price_list_index') }}">back to list</a>

    <a href="{{ path('app_price_list_edit', {'id': price_list.id}) }}">edit</a>

    {{ include('price_list/_delete_form.html.twig') }}
{% endblock %}
