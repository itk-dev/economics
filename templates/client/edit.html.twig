{% extends 'base.html.twig' %}

{% block title %}{{ 'client.edit'|trans}}{% endblock %}

{% block content %}
    <h1>{{ 'client.edit'|trans}}</h1>

    {{ include('client/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('client/_delete_form.html.twig') }}
    <a href="{{ path('app_client_index') }}">{{ 'client.back_to_list'|trans}}</a>
{% endblock %}
