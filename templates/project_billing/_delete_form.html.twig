<form method="post" action="{{ path('app_project_billing_delete', {viewId: viewId, 'id': projectBilling.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projectBilling.id) }}">
    <button class="button button-danger">{{ 'project_billing.action_delete'|trans }}</button>
</form>
